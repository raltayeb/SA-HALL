# 📋 كيفية إضافة قاعات مميزة

## المشكلة:
القاعات المميزة لا تظهر في الصفحة الرئيسية لأن **لا توجد قاعات مميزة في قاعدة البيانات بعد**.

---

## ✅ الحل 1: عبر SQL (الأسرع للتجربة)

### الخطوة 1: افتح Supabase SQL Editor
1. اذهب إلى https://supabase.com/dashboard
2. اختر مشروعك
3. من القائمة الجانبية اختر **SQL Editor**
4. انقر **New Query**

### الخطوة 2: اعرض القاعات المتاحة
```sql
SELECT id, name, city FROM halls WHERE is_active = true;
```

### الخطوة 3: انسخ ID القاعة
انسخ الـ ID من القاعة اللي تريد (مثال: `123e4567-e89b-12d3-a456-426614174000`)

### الخطوة 4: استبدل ID واضغط Run
```sql
-- اجعل القاعة مميزة لمدة 30 يوم
UPDATE halls 
SET is_featured = true, featured_until = NOW() + INTERVAL '30 days'
WHERE id = 'YOUR_HALL_ID_HERE';

INSERT INTO featured_halls (hall_id, end_date, is_active)
VALUES ('YOUR_HALL_ID_HERE', NOW() + INTERVAL '30 days', true);
```

### الخطوة 5: افتح الصفحة الرئيسية
القاعات المميزة ستظهر الآن! 🎉

---

## ✅ الحل 2: عبر لوحة السوبر أدمن

### الخطوة 1: سجل دخول كـ Super Admin

### الخطوة 2: اذهب إلى "القاعات المميزة"
من القائمة الجانبية

### الخطوة 3: انقر "إضافة قاعة مميزة"

### الخطوة 4: اختر القاعة والمدة
- ابحث عن القاعة
- اختر المدة (7, 14, 30, 60, 90, 180 يوم)
- انقر "تفعيل القاعة كمميزة"

### الخطوة 5: تحقق من الصفحة الرئيسية

---

## 🎯 كيف تعمل القاعات المميزة:

### في قاعدة البيانات:
```sql
-- جدول halls
is_featured = true
featured_until = '2026-03-23' -- تاريخ الانتهاء

-- جدول featured_halls
hall_id = 'xxx'
end_date = '2026-03-23'
is_active = true
```

### في الصفحة الرئيسية:
```typescript
// Home.tsx
const { data: featuredData } = await supabase
  .from('halls')
  .select('*')
  .eq('is_active', true)
  .eq('is_featured', true)
  .gt('featured_until', new Date().toISOString()) // لم ينتهي بعد
  .limit(3); // 3 قاعات فقط
```

### الانتهاء التلقائي:
```sql
-- دالة تنتهي تلقائياً
UPDATE halls
SET is_featured = FALSE
WHERE featured_until < NOW();
```

---

## 📊 القسم في الصفحة الرئيسية:

### عندما توجد قاعات مميزة:
```
┌────────────────────────────────────────┐
│      ⭐ قاعات مميزة - مختارة بعناية    │
├──────────┬──────────┬─────────────────┤
│ قاعة 1   │ قاعة 2   │ قاعة 3          │
│ صورة     │ صورة     │ صورة            │
│ السعر    │ السعر    │ السعر           │
└──────────┴──────────┴─────────────────┘
```

### عندما لا توجد قاعات مميزة:
```
┌────────────────────────────────────────┐
│      ⭐ قاعات مميزة - مختارة بعناية    │
├────────────────────────────────────────┤
│            ⭐ (رمز نجمة)                │
│     لا توجد قاعات مميزة حالياً          │
│  ستظهر هنا عند إضافتها من قبل الإدارة   │
└────────────────────────────────────────┘
```

---

## 🔍 اختبار:

### SQL للتأكد من الإضافة:
```sql
-- تأكد من القاعات المميزة
SELECT name, is_featured, featured_until 
FROM halls 
WHERE is_featured = true;

-- تأكد من جدول featured_halls
SELECT * FROM featured_halls WHERE is_active = true;

-- اختبر الاستعلام اللي في الصفحة الرئيسية
SELECT * FROM halls
WHERE is_active = true
  AND is_featured = true
  AND featured_until > NOW()
LIMIT 3;
```

### في المتصفح:
```javascript
// افتح Console واضع:
const { data } = await supabase
  .from('halls')
  .select('*')
  .eq('is_active', true)
  .eq('is_featured', true)
  .gt('featured_until', new Date().toISOString());

console.log(data); // يجب أن ترى القاعات
```

---

## ⚠️ مشاكل شائعة:

### ❌ "لا توجد قاعات مميزة حالياً"
**السبب:** لا توجد قاعات في قاعدة البيانات  
**الحل:** أضف قاعة مميزة عبر SQL أو لوحة التحكم

### ❌ القاعات تظهر ثم تختفي فجأة
**السبب:** `featured_until` منتهي  
**الحل:** حدث التاريخ: `UPDATE halls SET featured_until = NOW() + INTERVAL '30 days' WHERE id = 'xxx'`

### ❌ القسم لا يظهر أبداً
**السبب:** الكود لم يُحدث  
**الحل:** تأكد من تحديث `Home.tsx` وتشغيل `npm run dev`

---

## 📁 الملفات المعدلة:

| ملف | التغيير |
|-----|---------|
| `pages/Home.tsx` | إضافة قسم "قاعات مميزة" دائماً |
| `components/FeaturedHalls/AddFeaturedHallModal.tsx` | مودال إضافة قاعة |
| `db_vendor_subscription_fixed.sql` | جداول قاعدة البيانات |

---

**القاعات المميزة جاهزة! فقط أضف قاعة عبر SQL أو لوحة التحكم 🎉**
